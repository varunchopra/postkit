name: CI

on:
  push:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      max-parallel: 1
      matrix:
        postgres: [14, 15, 16, 17, 18]
        python: ["3.11", "3.12", "3.13", "3.14"]

    services:
      postgres:
        image: postgres:${{ matrix.postgres }}
        env:
          POSTGRES_PASSWORD: postgres
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      - uses: astral-sh/setup-uv@fac544c07dec837d0ccb6301d7b5580bf5edae39 # v8.2.0
        with:
          python-version: ${{ matrix.python }}

      # max_prepared_transactions needs a restart and service containers
      # take no command args; without it the prepared-transaction tests skip
      - name: Enable prepared transactions
        run: |
          docker exec ${{ job.services.postgres.id }} \
            psql -U postgres -c "ALTER SYSTEM SET max_prepared_transactions = 5"
          docker restart ${{ job.services.postgres.id }}
          until docker exec ${{ job.services.postgres.id }} pg_isready -U postgres; do sleep 1; done

      - run: make build test
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/postgres

  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - uses: astral-sh/setup-uv@fac544c07dec837d0ccb6301d7b5580bf5edae39 # v8.2.0
      - run: make lint
      - name: Check docs are up to date
        run: |
          make docs
          if [ -n "$(git status --porcelain docs/)" ]; then
            echo "::error::Docs are out of date. Run 'make docs' and commit."
            git diff docs/
            exit 1
          fi
